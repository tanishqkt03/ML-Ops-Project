

class ModelNameConfig():
    """
    Configuration for the model name used in the training step.
    This class allows you to specify the model name as a parameter,
    """
    # model_name: str = os.environ.get("MODEL_NAME", "LinearRegression")
    model_name: str = "LinearRegression"
    
    
    